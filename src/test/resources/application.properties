# 集成测试：显式绑定存储路径（避免仅依赖主 yml 时个别环境下未注入）
app.storage.root=./data
app.storage.chat-history=${app.storage.root}/history
app.storage.rag-docs=${app.storage.root}/knowledge
app.storage.vector-db=${app.storage.root}/vector-store
app.storage.mcp-map-sandbox=${app.storage.root}/mcp-map-sandbox
# 不拉起 MCP stdio 子进程
spring.ai.mcp.client.enabled=false
